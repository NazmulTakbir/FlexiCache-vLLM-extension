### First, clone the LEval repository into the L-Eval directory
```
cd benchmarks/language_modelling/L-Eval
git clone git@github.com:NazmulTakbir/LEval.git
```

### Next, install the required dependencies
```
pip install nltk
pip install rouge_score
pip install evaluate
```